apiVersion: v1
kind: Service
metadata:
  name: ohyes2
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "ohyes2"
    port: 8080
  selector:
    app: ohyes2
